<Window x:Class="PV178_Project.Views.Windows.GenerateMatchesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:PV178_Project.Views.Windows"
        mc:Ignorable="d"
        Title="GenerateMatchesWindow" Height="250" Width="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"   Grid.Column="0" Text="{Binding ActualTeamsCount}"/>
        <TextBlock Grid.Row="0"  Grid.Column="1"  Text="/"/>
        <TextBlock Grid.Row="0"  Grid.Column="2"  Text="{Binding ExpectedTeamsCount}" Foreground="Black"/>
        <Button Grid.Row="1" Grid.Column="1"  Command="{Binding GenerateMatchesCommand}" 
                Background="Aqua"
                IsEnabled="True"
                Style="{StaticResource NavigationButton}"> Generate </Button>

    </Grid>
</Window>
