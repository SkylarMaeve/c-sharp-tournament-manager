<Page x:Class="PV178_Project.Views.PlayersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="PlayersPage"
      HorizontalAlignment="Left"
      Background="{StaticResource Background}">
    <Grid Margin="50,50,0,0" HorizontalAlignment="Left">
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- Page Header -->
        <TextBlock
            Text="{Binding Header}"
            Grid.Row="0"
            HorizontalAlignment="Left"
            Style="{StaticResource StandardHeader}" />
        <Button
            Content="Add Player"
            Grid.Row="0"
            HorizontalAlignment="Right"
            IsEnabled="{Binding CanAddTeam}"
            Command="{Binding EditPlayerCommand}"
            Style="{StaticResource StandardButton}" />
        <!-- Columns Header -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" /><!-- Name -->
                <ColumnDefinition Width="200" /><!-- Team Name -->
                <ColumnDefinition Width="100" /><!-- DoB -->
                <ColumnDefinition Width="50" /><!-- Edit -->
                <ColumnDefinition Width="50" /><!-- Delete -->
            </Grid.ColumnDefinitions>
            <TextBlock
                Text="Name"
                Grid.Column="0"
                Style="{StaticResource ColumnHeader}" />
            <TextBlock
                Text="Team Name"
                Grid.Column="1"
                Style="{StaticResource ColumnHeader}" />
            <TextBlock
                Text="DoB"
                Grid.Column="2"
                Style="{StaticResource ColumnHeader}" />
            <TextBlock
                Text="Actions"
                Grid.Column="3"
                Grid.ColumnSpan="2"
                Style="{StaticResource ColumnHeader}" />
        </Grid>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Players}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200" /><!-- Name -->
                                <ColumnDefinition Width="200" /><!-- Team Name -->
                                <ColumnDefinition Width="100" /><!-- DoB -->
                                <ColumnDefinition Width="50" /><!-- Edit -->
                                <ColumnDefinition Width="50" /><!-- Delete -->
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Text="{Binding Name}"
                                Grid.Column="0"
                                Style="{StaticResource StandardTextBlock}" />
                            <TextBlock
                                Text="{Binding Team}"
                                Grid.Column="1"
                                Style="{StaticResource StandardTextBlock}" />
                            <TextBlock
                                Text="{Binding DateOfBirth, StringFormat='{}{0:dd.MM.yyyy}'}"
                                Grid.Column="2"
                                Style="{StaticResource StandardTextBlock}" />
                            <Button
                                Grid.Column="3"
                                Width="40"
                                Command="{Binding DataContext.EditPlayerCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}"
                                Style="{StaticResource StandardButton}">
                                <TextBlock
                                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                                    Text="&#xE70F;" />
                            </Button>

                            <Button
                                Grid.Column="4"
                                Width="40"
                                Background="{StaticResource DeleteButton}"
                                Command="{Binding DataContext.DeletePlayerCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}"
                                Style="{StaticResource StandardButton}">
                                <TextBlock
                                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                                    Text="&#xE74D;" />
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>

</Page>